const johnTeamScores=[89 ,120,103];
const mikeTeamScores=[116 ,94,123];
function calcAvrag(numArray){
    let sum=0;
for (let i = 0; i < numArray.length; i++) {
    sum += numArray[i];
}
return (sum/numArray.length).toFixed(3);
}


function 
